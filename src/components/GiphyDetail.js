import React from 'react'

const GiphyDetail = (props) => {
    console.log(props)
    // return <div><img className="ui image" src={props.giphy.images.original.url} alt={props.giphy.title} /></div>
    return <div>null</div>
}

export default GiphyDetail